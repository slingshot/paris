---
'paris': minor
---

Tabs: panels flow with the page instead of scrolling inside the tab group. Consumers that relied on the group or panel being a scroll container should wrap Tabs in their own scroll container.
