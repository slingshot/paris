---
"paris": minor
---

Portal Menu, Select, and Combobox dropdowns to document body via Headless UI's anchor prop, fixing z-index stacking issues where dropdowns appeared behind other content due to parent stacking contexts. Add semantic z-index layer tokens (Theme.new.layers) with six named layers: below, sticky, dropdown, overlay, popover, menu. Fix SegmentedControl labels bleeding through GlassTabs sticky bar.
